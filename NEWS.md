# ProstateAtlasMapR 0.0.0.9000

* Initial development version.
* `build_reference()`, `get_reference()`, `map_query()`, `plot_query_mapping()`.
* `map_query()` auto-joins Seurat v5 split layers and auto-picks query assay
  when `query_assay = NULL`.
* Lean reference data is distributed outside the package (Releases / Zenodo).
