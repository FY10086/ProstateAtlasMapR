% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rm.R
\name{plot_rm_prediction}
\alias{plot_rm_prediction}
\title{DimPlot of predicted labels with low-score cells as Unknown}
\usage{
plot_rm_prediction(
  query,
  annotation_level = c("H1", "H2", "H3"),
  score_threshold = 0.6,
  reduction = "umap.rpca"
)
}
\arguments{
\item{query}{Seurat object after \code{\link[=map_query]{map_query()}}.}

\item{annotation_level}{\code{"H1"}, \code{"H2"}, or \code{"H3"}.}

\item{score_threshold}{Score cutoff for Unknown. Default \code{0.6}.
Use \code{NULL} to keep all predicted labels.}

\item{reduction}{Dimensional reduction to plot. Default \code{"umap.rpca"}.}
}
\value{
A ggplot object (H1/H2) or a cowplot / ggplot grob (H3).
}
\description{
Cells with prediction score below \code{score_threshold} are labelled
\code{"Unknown"} (grey). Set \code{score_threshold = NULL} to skip thresholding.
}
\details{
\strong{H1 / H2:} compact Seurat \code{DimPlot} with legend on the right
(\code{repr.plot} 12 x 10).
\strong{H3:} same style as \code{09-RM.ipynb} — UMAP and legend are split with
\code{cowplot::plot_grid()} (\code{repr.plot} 22 x 10) so long subtype names
do not crush the map.
}
