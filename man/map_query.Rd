% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_query.R
\name{map_query}
\alias{map_query}
\title{Map a query Seurat object onto the prostate atlas reference}
\usage{
map_query(
  reference,
  query,
  annotation_level = c("H1", "H2", "H3"),
  query_assay = NULL,
  dims = NULL,
  normalization.method = "LogNormalize",
  k.anchor = 5,
  k.score = 30,
  k.weight = 100,
  verbose = TRUE
)
}
\arguments{
\item{reference}{A \code{patlas_reference} object (from \code{\link[=build_reference]{build_reference()}} or
\code{\link[=get_reference]{get_reference()}}), or a path to a \code{.qs} file containing one.}

\item{query}{A \code{Seurat} object to map onto the reference.}

\item{annotation_level}{Which annotation level to transfer: one of
\code{"H1"}, \code{"H2"}, \code{"H3"}. Must be one of \code{reference$annotation_levels}.}

\item{query_assay}{Assay in \code{query} holding gene expression. If \code{NULL}
(default), uses \code{"RNA"} when present, otherwise \code{\link[Seurat:reexports]{Seurat::DefaultAssay()}}.
For Seurat sketch objects this is often \code{"sketch"}.}

\item{dims}{Number of reference dimensions to use for anchoring. Defaults
to \code{reference$npcs}.}

\item{normalization.method}{Passed to \code{\link[Seurat:FindTransferAnchors]{Seurat::FindTransferAnchors()}};
\code{query} is normalized automatically with this method if it has not
been already.}

\item{k.anchor, k.score}{Passed to \code{\link[Seurat:FindTransferAnchors]{Seurat::FindTransferAnchors()}}.}

\item{k.weight}{Passed to \code{\link[Seurat:MapQuery]{Seurat::MapQuery()}} / \code{TransferData()}.}

\item{verbose}{Print progress messages.}
}
\value{
The \code{query} Seurat object with two new columns added to
\code{meta.data} (\verb{predicted.celltype_manual_H\{level\}} and
\verb{predicted.celltype_manual_H\{level\}.score}), and a new dimensional
reduction -- named the same as \code{reference$umap_reduction} (e.g.
\code{"umap.rpca"}) -- holding the query cells projected into the
reference's published UMAP coordinate system.
}
\description{
Runs Seurat's standard anchor-based reference-mapping workflow
(\code{\link[Seurat:FindTransferAnchors]{Seurat::FindTransferAnchors()}} + \code{\link[Seurat:MapQuery]{Seurat::MapQuery()}}) to transfer cell
type labels from the reference atlas onto a new (query) single-cell
dataset, and to project the query cells into the reference's published
UMAP coordinate system.
}
