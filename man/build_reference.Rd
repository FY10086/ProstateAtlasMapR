% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_reference.R
\name{build_reference}
\alias{build_reference}
\title{Build a lightweight, distributable reference from the prostate atlas}
\usage{
build_reference(
  qc_path,
  reduction = "rpca",
  umap_reduction = "umap.rpca",
  npcs = 50,
  n.neighbors = 30,
  metric = "cosine",
  annotation_levels = c("H1", "H2", "H3"),
  keep_meta_cols = NULL,
  assay = "RNA",
  save_path = NULL,
  verbose = TRUE
)
}
\arguments{
\item{qc_path}{Path to the source Seurat object (\code{.qs} file), typically
the full prostate atlas \code{QC_fin.qs}.}

\item{reduction}{Name of the dimensional reduction with feature loadings
used to anchor/project new query cells. Default \code{"rpca"}.}

\item{umap_reduction}{Name of the UMAP reduction to attach a projection
model to; its existing coordinates are preserved exactly. Default
\code{"umap.rpca"}.}

\item{npcs}{Number of dimensions of \code{reduction} to use. Default \code{50}.}

\item{n.neighbors, metric}{Only used to build the neighbor structure that
supports projecting \emph{new} query cells; they do not affect the existing
reference cells' coordinates. Defaults match \code{\link[Seurat:RunUMAP]{Seurat::RunUMAP()}}.}

\item{annotation_levels}{Which annotation level(s) (\code{celltype_manual_H1}
/ \code{H2} / \code{H3}) to retain in the reference. All three are kept by
default so a single reference file supports every level; the level to
use for a given mapping call is chosen later in \code{\link[=map_query]{map_query()}}.}

\item{keep_meta_cols}{Additional \code{meta.data} columns to retain besides
the annotation columns (e.g. \code{"sample.ID"}). \code{NULL} by default.}

\item{assay}{Assay holding gene expression. Default \code{"RNA"}.}

\item{save_path}{If not \code{NULL}, save the resulting reference with
\code{qs::qsave(..., preset = "high")} to this path.}

\item{verbose}{Print progress messages.}
}
\value{
An object of class \code{patlas_reference}.
}
\description{
Loads the full prostate single-cell atlas (typically \code{QC_fin.qs}),
attaches a projection-capable model to the existing UMAP reduction
(\code{umap.rpca} by default) \strong{without altering its coordinates}, strips the
object down to the minimum needed for Seurat's anchor-based reference
mapping workflow, and optionally caches the lean result to disk. This
lean object -- not the original multi-gigabyte atlas -- is the artifact
meant to be shared/distributed (see \code{\link[=get_reference]{get_reference()}}).
}
\details{
The atlas's \code{umap.rpca} reflects a round of post-hoc, low-quality-cell
filtering performed \emph{after} the UMAP embedding was computed, so its
coordinates cannot be reproduced by simply re-running
\code{\link[Seurat:RunUMAP]{Seurat::RunUMAP()}} on the current cell set. Instead, this function
attaches a \code{uwot} transform model built with \verb{init = <existing coordinates>} and \code{n_epochs = 0}, which returns the supplied coordinates
completely unchanged while still building the nearest-neighbor structure
required to embed brand-new query cells later on.
}
