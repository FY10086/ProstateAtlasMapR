% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_query_mapping.R
\name{plot_query_mapping}
\alias{plot_query_mapping}
\title{Visualize a mapped query alongside the reference atlas}
\usage{
plot_query_mapping(
  reference,
  query,
  annotation_level = c("H1", "H2", "H3"),
  pt.size.reference = 0.1,
  pt.size.query = 0.6,
  ...
)
}
\arguments{
\item{reference}{A \code{patlas_reference} object, or a path to one.}

\item{query}{A query \code{Seurat} object that has already been processed by
\code{\link[=map_query]{map_query()}} (i.e. it has a reduction named \code{reference$umap_reduction}
and a \verb{predicted.<annotation column>} metadata column).}

\item{annotation_level}{Which annotation level was used for mapping. One
of \code{"H1"}, \code{"H2"}, \code{"H3"}.}

\item{pt.size.reference, pt.size.query}{Point sizes for the reference and
query cells, passed to \code{\link[Seurat:DimPlot]{Seurat::DimPlot()}}.}

\item{...}{Additional arguments passed to \code{\link[Seurat:DimPlot]{Seurat::DimPlot()}}.}
}
\value{
A \code{patchwork}/\code{ggplot} object with two side-by-side panels:
reference cells and query cells on the same UMAP coordinates.
}
\description{
Draws the reference cells (colored by the chosen annotation level) and
the query cells (colored by their predicted labels) on the same UMAP
coordinate system produced by \code{\link[=map_query]{map_query()}}, for a quick visual sanity
check of the mapping result.
}
