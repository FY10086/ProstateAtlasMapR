% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_rctd.R
\name{run_rctd}
\alias{run_rctd}
\title{Run RCTD spatial deconvolution against the prostate atlas}
\usage{
run_rctd(
  reference,
  spatial,
  annotation_level = c("H1", "H2", "H3"),
  assay = NULL,
  image = NULL,
  coords = NULL,
  use_fake_coords = FALSE,
  doublet_mode = c("doublet", "full", "multi"),
  max_cores = 4,
  n_max_cells = 10000,
  min_UMI = 100,
  CELL_MIN_INSTANCE = 25,
  gene_cutoff = 0.000125,
  fc_cutoff = 0.5,
  gene_cutoff_reg = 2e-04,
  fc_cutoff_reg = 0.75,
  UMI_min = 100,
  UMI_max = 2e+07,
  counts_MIN = 10,
  UMI_min_sigma = 300,
  CONFIDENCE_THRESHOLD = 5,
  DOUBLET_THRESHOLD = 20,
  MAX_MULTI_TYPES = 4,
  class_level = NULL,
  assay_name = "RCTD",
  store_rctd = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{reference}{A \code{patlas_reference}, a path to one, a Seurat object with
atlas annotations, or a path to such a Seurat \code{.qs} file.}

\item{spatial}{A Seurat object holding spatial transcriptomics data
(Visium / other spot-based assays with tissue coordinates).}

\item{annotation_level}{Which atlas level to use as RCTD cell types:
\code{"H1"}, \code{"H2"}, or \code{"H3"}.}

\item{assay}{Assay in \code{spatial} with raw counts. If \code{NULL}, uses \code{"Spatial"}
when present, otherwise \code{\link[Seurat:reexports]{Seurat::DefaultAssay()}}.}

\item{image}{Name of the Seurat image to pull coordinates from; passed to
\code{\link[Seurat:reexports]{Seurat::GetTissueCoordinates()}}. \code{NULL} uses the default image. Ignored
when \code{coords} is supplied.}

\item{coords}{Spot coordinates. Preferred: a \strong{reduction name} in
\code{spatial} (e.g. \code{"spatial"}), using the first two dimensions as \code{x}/\code{y}.
Alternatively a \code{data.frame}/\code{matrix} with columns \code{x} and \code{y} and
rownames matching \code{colnames(spatial)}. If \code{NULL}, falls back to
\code{\link[Seurat:reexports]{Seurat::GetTissueCoordinates()}}.}

\item{use_fake_coords}{If \code{TRUE}, ignore real coordinates and use a
placeholder grid (see \code{\link[spacexr:SpatialRNA]{spacexr::SpatialRNA()}}). Default \code{FALSE}.}

\item{doublet_mode}{RCTD mode passed to \code{\link[spacexr:run.RCTD]{spacexr::run.RCTD()}}:
\code{"doublet"} (default; Visium-like), \code{"full"}, or \code{"multi"}.}

\item{max_cores}{Parallel cores for \code{\link[spacexr:create.RCTD]{spacexr::create.RCTD()}}. Default \code{4}.}

\item{n_max_cells}{Max cells kept per cell type when building the spacexr
\code{Reference} (downsampling). Default \code{10000}.}

\item{min_UMI}{Minimum UMI for reference cells. Default \code{100}.}

\item{CELL_MIN_INSTANCE}{Minimum cells required per cell type in
\code{\link[spacexr:create.RCTD]{spacexr::create.RCTD()}}. Default \code{25}. Lower for rare H3 types if needed.}

\item{gene_cutoff, fc_cutoff, gene_cutoff_reg, fc_cutoff_reg, UMI_min, UMI_max, counts_MIN, UMI_min_sigma, CONFIDENCE_THRESHOLD, DOUBLET_THRESHOLD, MAX_MULTI_TYPES}{Passed through to \code{\link[spacexr:create.RCTD]{spacexr::create.RCTD()}}.}

\item{class_level}{Optional coarser annotation level (\code{"H1"} / \code{"H2"} /
\code{"H3"}) used to build spacexr's \code{class_df} so RCTD can report
class-level confidence. Must differ from \code{annotation_level} and be
present in \code{reference}. \code{NULL} (default) skips \code{class_df}.}

\item{assay_name}{Name of the new assay written onto \code{spatial} that holds
cell-type weights (features = cell types, cells = spots). Default
\code{"RCTD"}. Set to \code{NULL} to skip writing an assay.}

\item{store_rctd}{If \code{TRUE} (default), store the full spacexr \code{RCTD}
object in \code{spatial@misc$rctd}.}

\item{verbose}{Print progress messages.}
}
\value{
The \code{spatial} Seurat object with:
\itemize{
\item meta.data columns \code{rctd_spot_class}, \code{rctd_first_type},
\code{rctd_second_type} (doublet mode; \code{NA} when unavailable);
\item \verb{@misc$rctd_weights}: spots x cell-types matrix (original type names);
\item an assay of weights named \code{assay_name} (when not \code{NULL}; \verb{_} in type
names is replaced by \code{-} to satisfy Seurat feature-name rules);
\item optionally the raw \code{RCTD} object in \verb{@misc$rctd}.
}
}
\description{
Wraps \code{\link[spacexr:create.RCTD]{spacexr::create.RCTD()}} + \code{\link[spacexr:run.RCTD]{spacexr::run.RCTD()}} using the same
prostate atlas reference as \code{\link[=map_query]{map_query()}}. Choose which annotation level
(\code{H1} / \code{H2} / \code{H3}) to use as the RCTD cell-type labels.
}
\details{
The lean \code{patlas_reference} (from \code{\link[=build_reference]{build_reference()}} / \code{\link[=get_reference]{get_reference()}})
keeps the ~2,000 anchor genes used for Seurat mapping. That gene set is
usually enough for RCTD, but if you need a fuller transcriptome you can
pass a Seurat object (or \code{.qs} path) that still has \code{counts} plus the
\verb{celltype_manual_H*} columns.

Spatial coordinates resolution order:
\enumerate{
\item \code{coords} as a reduction name (e.g. \code{"spatial"}) → first two embedding
dimensions;
\item \code{coords} as a \code{data.frame}/\code{matrix} with columns \code{x}/\code{y};
\item otherwise \code{\link[Seurat:reexports]{Seurat::GetTissueCoordinates()}} (optionally via \code{image}).
Set \code{use_fake_coords = TRUE} only when you want proportions without a real
spatial layout (coords are still required by spacexr internally).
}
}
