% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rctd.R
\name{plot_rctd_first_type}
\alias{plot_rctd_first_type}
\title{Spatial map of RCTD dominant cell type (first_type)}
\usage{
plot_rctd_first_type(
  spatial,
  annotation_level = NULL,
  reduction = "physical",
  min_pct = 0,
  reverse_y = TRUE
)
}
\arguments{
\item{spatial}{Seurat object after \code{\link[=run_rctd]{run_rctd()}}.}

\item{annotation_level}{Atlas level \code{"H1"}, \code{"H2"}, or \code{"H3"}. Default
\code{NULL} uses the level stored by the last \code{\link[=run_rctd]{run_rctd()}} call (else \code{"H2"}).}

\item{reduction}{Spatial coordinates reduction. Default \code{"physical"}.}

\item{min_pct}{Merge types below this percent of spots into Other.
Default \code{0} (no merge).}

\item{reverse_y}{Flip the y axis (common for Visium). Default \code{TRUE}.}
}
\value{
A ggplot object (H1/H2) or cowplot grob (H3).
}
\description{
Reject / missing calls are shown as \code{"Unknown"}. Rare types below
\code{min_pct} of spots can be merged into \code{"Other"}.
}
\details{
\strong{H1 / H2:} original compact map with legend on the right
(\code{repr.plot} 12 x 10).
\strong{H3:} map and legend split with measured legend width (\code{repr.plot}
height-matched) so long subtype names do not crush the tissue or leave a
large gap beside \code{coord_fixed} spatial panels.
}
